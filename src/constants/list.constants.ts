const LIMIT_LIST = [
  { label: "8", value: 8 },
  { label: "12", value: 12 },
  { label: "16", value: 16 },
];

const DELAY = 1000;

const LIMIT_DEFAULT = LIMIT_LIST[0].value;

const PAGE_DEFAULT = 1;

const LIMIT_BANNER = 5;
const LIMIT_TEACHER = 5;
const LIMIT_EVENT = 4;

export {
  LIMIT_LIST,
  DELAY,
  LIMIT_DEFAULT,
  PAGE_DEFAULT,
  LIMIT_BANNER,
  LIMIT_TEACHER,
  LIMIT_EVENT,
};
